import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    txtG: {
        fontSize: 32
    },
    displayFlax: {
        display: "flex",
        backgroundColor: "#000"
    }
})